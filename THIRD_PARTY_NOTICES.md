# Third-party notices

The project integrates with OBS Studio and downloads or uses components from
whisper.cpp, FFmpeg and official Whisper model publishers. These components are
not relicensed by this repository; their respective licenses and notices apply.

The Linux desktop package uses Electron and its npm dependencies, each under the
license declared by its publisher. See `linux/package-lock.json` for the exact
dependency versions.
